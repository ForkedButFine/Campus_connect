
  # Campus Connect Website

  This is a code bundle for Campus Connect Website. The original project is available at Campus-Connect-Website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  